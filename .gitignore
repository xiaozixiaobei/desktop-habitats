.DS_Store
.claude/
node_modules/
/aquarium-shader-improved.zip

# Release output: the staged app, the icon set and the disk image itself.
/build/
# Generated by wallpaper/artwork.swift on the way to AppIcon.icns.
/packaging/dmg-background.png
# Agent workspace memory, kept beside the project so it travels with it, never shipped.
/.workbuddy/
